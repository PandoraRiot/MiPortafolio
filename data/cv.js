/**
 * CV download for the hero "CV" button.
 *
 * TODO(Alexa): Add final CV PDF.
 *   1. Put the PDF(s) in assets/cv/ (e.g. assets/cv/CV_Alexandra_Garcia_EN.pdf).
 *      Check first that you are OK publishing everything in it (phone number!).
 *   2. Write the relative path below. `es` falls back to `en` and vice versa.
 *
 * While both are empty the CV button stays hidden — no broken link, no
 * invented URL.
 */
window.PORTFOLIO_CV = {
  en: '',
  es: '',
};
